{
  "name": "vscode-browser-bridge",
  "displayName": "VS Code Browser Bridge",
  "description": "MCP bridge between VS Code's integrated browser and external agents like Claude Code",
  "version": "0.0.1",
  "publisher": "defrog",
  "engines": {
    "vscode": "^1.110.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "browserBridge.start",
        "title": "Browser Bridge: Start"
      },
      {
        "command": "browserBridge.stop",
        "title": "Browser Bridge: Stop"
      },
      {
        "command": "browserBridge.status",
        "title": "Browser Bridge: Show Status"
      }
    ],
    "configuration": {
      "title": "Browser Bridge",
      "properties": {
        "browserBridge.httpPort": {
          "type": "number",
          "default": 3788,
          "description": "Port for the local HTTP API server"
        },
        "browserBridge.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "Automatically start the bridge when VS Code opens"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && node esbuild.js --production",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^4.21.2",
    "ws": "^8.18.2",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.110.0",
    "@types/ws": "^8.18.1",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  }
}
